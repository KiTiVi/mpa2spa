{
  "name": "mpa2spa",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-react": "cd react && npm i",
    "build-react": "cd react && npm run build",
    "start-server": "npm i && nodemon server.js",
    "start": "npm run install-react && npm run build-react && npm run start-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.0.0",
    "env-cmd": "^9.0.2",
    "express": "^4.17.1",
    "nodemon": "^1.19.1"
  }
}
